apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: aiboard

resources:
  - namespace.yaml
  - postgres.yaml
  - app.yaml
  # Uncomment to enable ingress:
  # - ingress.yaml

images:
  - name: ghcr.io/d6o/aiboard
    newTag: latest
